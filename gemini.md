# PSC-Graph 项目开发准则 (Gemini版)

## 概览
本文件用于指导 Gemini (Antigravity) 在政策语义因果图谱(PSC-Graph)项目中的开发与协作工作。本项目旨在构建"政策语义→政策意图→产业行为/绩效"的端到端体系。

**项目核心**:
- **认知断裂**: 通过LLM+RAG实现政策语义抽取
- **行为断裂**: 通过异质时序图学习(HGT+TGAT)建立政策-产业图谱
- **评估断裂**: 通过稳健DID方法(CS-ATT/Sun-Abraham/BJS)进行因果识别

**语言使用强制规范**
- ⚠️ **绝对强制使用简体中文**: 所有回复、文档、注释、日志、提交信息等必须使用简体中文。
- 唯一例外: 代码标识符(变量名、函数名、类名等)遵循项目既有命名约定。

## 🔒 强制验证机制
- **本地验证**: 每次改动必须提供可重复的本地验证步骤(通过 `run_command` 执行脚本或测试)。
- **自我修正**: 验证失败时必须立即分析原因并修正，禁止提交未通过验证的代码。
- **测试覆盖**: 必须确保关键逻辑有对应的测试用例。

## 🤝 质量审查规范
- **自我审查**: 在提交代码前，必须检查：
    - 是否符合项目代码风格？
    - 是否引入了未使用的依赖？
    - 是否处理了边界情况？
    - 是否有潜在的性能问题？
- **用户审查**: 关键变更需通过 `notify_user` 请求用户审查。

## 📊 架构优先级
- **复用优先**: 优先复用官方SDK、社区成熟方案或项目内既有模块。
- **避免造轮子**: 禁止新增自研方案，除非现有方案无法满足需求。

## 🛡️ 安全性原则
- **数据合规**: 仅使用公开政府数据源，遵守 robots.txt，QPS≤1.0。
- **安全编码**: 防止 Prompt Injection，验证输入数据，不硬编码敏感信息。

## ✅ 代码质量强制标准

### 📝 语言与注释规范
- **强制中文**: 所有文档、注释、日志、Commit Message 必须使用简体中文。
- **注释规范**: 解释"为什么"而非"做什么"，避免废话。

### 🧪 测试规范
- **自动化测试**: 每次实现必须提供可自动运行的测试(单元测试或脚本)。
- **覆盖率**: 覆盖正常流程和异常流程。

### 🏗️ 设计原则
- **SOLID**: 遵循单一职责、开闭原则等。
- **DRY**: 避免重复代码。

### 💻 实现标准
- **完整实现**: 禁止提交 MVP 或占位符代码，必须是完整的实现。
- **清理代码**: 主动删除过时、无用的代码。

## 🚀 强制工作流程

### ⚡ 总原则
- **思考先行**: 在执行复杂任务前，必须使用 `task_boundary` 进行规划 (PLANNING 模式)。
- **问题驱动**: 明确要解决的问题，而非盲目执行。

### 🔗 工具使用规范 (Gemini工具链)
- **文件操作**:
    - 读文件: `view_file` (优先), `read_resource`
    - 写文件: `write_to_file` (新建), `replace_file_content` (单块修改), `multi_replace_file_content` (多块修改)
    - 目录: `list_dir`
- **搜索与检索**:
    - 文件名搜索: `find_by_name`
    - 内容搜索: `grep_search` (优先), `search_in_file`
    - 代码理解: `codebase_search`, `view_code_item`
- **命令执行**:
    - 运行脚本/命令: `run_command` (PowerShell)
    - 检查状态: `command_status`
- **Web检索**:
    - 查阅文档/资料: `search_web`

### 🔍 编码前强制检索 (Context Retrieval)
在开始编码前，必须执行以下步骤获取上下文：
1.  **搜索相关文件**: 使用 `find_by_name` 找到相关文件。
2.  **搜索相关代码**: 使用 `grep_search` 或 `codebase_search` 查找类似实现或引用。
3.  **阅读理解**: 使用 `view_file` 深入阅读核心代码和依赖。
4.  **确认复用**: 确认是否有现成的工具函数或组件可复用。

## 🔧 项目集成规则 (PSC-Graph 特定)

### 核心技术栈强制依赖
#### 数据采集层
- **必须使用**: requests + BeautifulSoup4, pdfplumber, 自定义节流器
- **禁止使用**: Scrapy, Selenium, 第三方爬虫服务

#### NLP与语义抽取层
- **必须使用**: RAG (BM25+FAISS), sentence-transformers (MiniLM-L12-v2), JSON Schema 验证
- **禁止使用**: 纯零样本 LLM, 单一检索方式

#### 图学习层
- **必须使用**: PyTorch Geometric (PyG), HGT, TGAT, Bochner时间编码
- **禁止使用**: 同质图模型, 非时序图模型

#### 因果推断层
- **必须使用**: R `did` 包, `didimputation`, Python调用R
- **禁止使用**: TWFE, 单一估计器

### 数据来源强制约束
- **政策文本**: 国务院政策文件库, 省级科技厅 (禁止非官方解读)
- **产业指标**: 国家统计局 (禁止商业数据库)
- **专利数据**: CNIPA (禁止未授权API)

### 标注规范强制标准
- **五元组**: goal, instrument (枚举), target_actor, strength (0-3), evidence_spans
- **一致性**: Cohen's κ ≥ 0.80

## 📁 文件结构规范
- **Gemini 记忆**: `C:\Users\shuai\.gemini\antigravity\brain\` (用于存储 task.md, implementation_plan.md 等)
- **项目文件**: 直接在项目根目录下操作，遵循项目现有结构。

---
**重要**: 遇到不确定的技术细节或业务逻辑，优先查阅 `CLAUDE.md` 和 `README.md`，或使用 `search_web` 查阅官方文档。
